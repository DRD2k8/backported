package com.drd.backported.item;

import net.minecraft.world.item.*;

public class SpearItem extends TieredItem {
    /**
     * Just so you know, making the spears is still a heavy WIP, so it will take a while to get the spear item models and the spear itself to work.
     */
    public SpearItem(Tier tier, Properties properties) {
        super(tier, properties);
    }
}
